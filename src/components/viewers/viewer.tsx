// Import your viewer and add it to the viewers object below with a unique ID

import { viewerMetadata as hexedit } from "./hexedit";
import { viewerMetadata as textedit } from "./textedit";

export const viewers = {
    "hexedit": hexedit,
    "textedit": textedit,
}